<Window x:Class="PresentationLayer.View.StateView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PresentationLayer.View"
        xmlns:vmodel="clr-namespace:PresentationLayer.ViewModel"
        mc:Ignorable="d"
        Title="StateView" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Label Grid.Column="0" Grid.Row="0" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10">Id:</Label>
        <Label Grid.Column="0" Grid.Row="2" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10">Number of books:</Label>
        <Label Grid.Column="0" Grid.Row="4" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10">Catalog:</Label>

        <TextBox Grid.Column="1" Grid.Row="1" Margin="15"  HorizontalAlignment="Stretch" VerticalAlignment="Center" Text="{Binding StateId}"/>
        <TextBox Grid.Column="1" Grid.Row="3" Margin="15"  HorizontalAlignment="Stretch" VerticalAlignment="Center" Text="{Binding NrOfBooks}"/>
        <TextBox Grid.Column="1" Grid.Row="5" Margin="15"  HorizontalAlignment="Stretch" VerticalAlignment="Center" Text="{Binding Catalog}"/>

        <Button x:Name="confirm" Content="Confirm" Grid.Column="0" Grid.Row="6" ClickMode="Press" Margin="10,10,10,10" />
    </Grid>
</Window>
